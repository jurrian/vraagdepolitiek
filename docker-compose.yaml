version: '3.1'

services:
  app:
    build:
      context: .
    ports:
      - "80:8000"
  db:
    image: mariadb
    restart: always
    ports:
      - "3306:3306"
    volumes:
      - "mysql-data:/var/lib/mysql"
    environment:
      MYSQL_ROOT_PASSWORD:
      MYSQL_RANDOM_ROOT_PASSWORD: "true"
      MYSQL_DATABASE: vragenvuur
      MYSQL_USER: vragenvuur
      MYSQL_PASSWORD: vragenvuur
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    ports:
    - "8080:80"
volumes:
  mysql-data:
